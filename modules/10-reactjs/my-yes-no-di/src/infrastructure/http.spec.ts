import { Http } from './http'
import { instance } from 'ts-mockito'
import { TYPES } from '../types'

describe('Http ', () => {
  describe('get', () => {
    it('should get response', () => {})
  })
})
